1.	Viết chương trình thực hiện các yêu cầu sau:
- Khai báo lớp phân số với các thuộc tính: tử số, mẫu số. Xây dựng hàm tạo không đối, hàm tạo có đối, hàm hủy một đối tượng phân số.
- Xây dựng các toán từ >>, << để nhập, xuất
- Xây dựng các phương thức: nhập, xuất, rút gọn một đối tượng phân số.
- Nạp chồng các toán tử: = (gán),> (lớn hơn),< (nhỏ hơn),
- Định nghĩa các toán tử +, -, *, / hai phân số. Thực hiện cộng, trừ, nhân, chia hai phân số (kết quả phải được tối giản) và in kết quả ra màn hình.
 - Viết chương trình chính ứng dụng lớp PS2 để nhập một danh sách các đối tượng là phân số (tối đa 10 phần tử). Sắp xếp lại danh sách đã nhập theo trật tự giảm dần.
- TÌm phần tử có giá trị lơn nhất, nhỏ nhất
-  Tính tổng các phân số trong danh sách có giá trị nhỏ hơn 3/4


2.	Viết chương trình thực hiện các yêu cầu sau:
- Khai báo lớp thí sinh với các thuộc tính: mã thí sinh, họ tên, điểm toán, điểm lý, điểm hóa.
- Xây dựng các phương thức: nhập, xuất, tính tổng điểm một đối tượng thí sinh.
- Nhập vào n thí sinh. In ra màn hình thông tin của thí sinh có tổng điểm cao nhất (yêu cầu sử dụng biến con trỏ để cấp phát và giải phóng bộ nhớ).


3.	Viết chương trình thực hiện các yêu cầu sau:
- Khai báo lớp đa thức với các thuộc tính: bậc đa thức, các hệ số tương ứng.
- Xây dựng các phương thức: nhập, xuất một đối tượng đa thức.
- Định nghĩa toán tử +, - hai đa thức. Thực hiện cộng, trừ hai đa thức và in kết quả ra màn hình.


4.	Viết chương trình thực hiện các yêu cầu sau:
- Khai báo lớp date với các thuộc tính: ngày, tháng, năm. Xây dựng hàm tạo không đối, hàm tạo có đối, hàm hủy một đối tượng date.
- Xây dựng các phương thức: nhập, xuất, chuẩn hóa một đối tượng date.
- Định nghĩa toán từ ++,-- để tăng, giảm một ngày. Thực hiện tăng, giảm một ngày và in kết quả ra màn hình (kết quả phải được chuẩn hóa dưới dạng dd-mm-yyyy).


5.	Viết chương trình thực hiện các yêu cầu sau:
- Khai báo lớp time với các thuộc tính: giờ, phút, giây. Xây dựng hàm tạo không đối, hàm tạo có đối, hàm hủy một đối tượng time.
- Xây dựng các toán từ >>, << để nhập, xuất
- Xây dựng các phương thức: nhập, xuất, chuẩn hóa một đối tượng time.
- Định nghĩa toán từ ++,-- để tăng, giảm một giây. Thực hiện tăng, giảm một giây và in kết quả ra màn hình (kết quả phải được chuẩn hóa dưới định dạng 24 tiếng hh:mm:ss).


6.	Viết chương trình thực hiện các yêu cầu sau:
- Khai báo lớp người với các thuộc tính: họ tên, năm sinh. Khai báo lớp sinh viên kế thừa từ lớp người và thêm các thuộc tính: mã sinh viên, điểm trung bình.
- Xây dựng các phương thức: nhập, xuất cho các đối tượng người, sinh viên.
- Nhập vào n sinh viên. Nhập thông tin tìm kiếm là mã sinh viên hoặc họ tên. In ra màn hình thông tin sinh viên tìm thấy hoặc thông báo là không tìm thấy.


7.	Viết chương trình thực hiện các yêu cầu sau:
- Khai báo lớp sinh viên với các thuộc tính: họ tên, năm sinh, điểm 5 môn học.
- Xây dựng các phương thức: nhập, xuất, tính điểm trung bình của một sinh viên.
- Nhập vào n sinh viên. In ra màn hình thông tin các sinh viên phải thi lại, tên môn thi lại và điểm thi tương ứng (môn thi lại có điểm thi dưới 5).


8.	Viết chương trình thực hiện các yêu cầu sau:
- Khai báo lớp ma trận vuông với các thuộc tính: cấp ma trận, các phần tử của ma trận.
- Xây dựng các phương thức: nhập, xuất một đối tượng ma trận.
- Xây dựng các toán từ >>, << để nhập, xuất
- Nhập và 1 ma trận. In ra màn hình ma trận vừa nhập. Tính tổng các phần tử nằm trên đường chéo chính, tổng các phần tử nằm trên đường chéo phụ, tổng các phần tử nằm trên đường viền của ma trận.
- Nhập vào hai ma trận đồng cấp. 
+ Thực hiện cộng hai ma trận vừa nhập (dùng hàm bạn). In ra màn hình hai ma trận ban đầu và ma trận kết quả
+ Thực hiện trừ hai ma trận vừa nhập (dùng hàm bạn). In ra màn hình hai ma trận ban đầu và ma trận kết quả


9.	Viết chương trình thực hiện các yêu cầu sau:
- Khai báo lớp nhân viên với các thuộc tính: họ tên, chức vụ, giới tính, năm sinh, địa chỉ.
- Xây dựng các phương thức: nhập, xuất, tính tuổi của một nhân viên (năm hiện tại trừ năm sinh).
- Nhập vào n nhân viên. In ra màn hình danh sách các nhân viên đủ tuổi về hưu (nam từ 60 tuổi trở lên, nữ từ 55 tuổi trở lên).


10.	Viết chương trình thực hiện các yêu cầu sau:
- Khai báo lớp thí sinh với các thuộc tính: số báo danh, họ tên, điểm toán, điểm lý, điểm hóa.
- Xây dựng các phương thức: nhập, xuất, tính tổng điểm của một thí sinh.
- Nhập vào n thí sinh. In ra màn hình thông tin các thí sinh có tổng điểm từ 21 trở lên và không có môn nào dưới 2.


11.	Viết chương trình thực hiện các yêu cầu sau:
- Khai báo lớp nhân viên với các thuộc tính: họ tên, chức vụ, giới tính, năm sinh, quê quán, năm vào làm.
- Xây dựng các toán tử >>, << để nhập, xuất một đối tượng nhân viên.
- Nhập vào n nhân viên. In ra màn hình danh sách các nhân viên làm việc trên 20 năm (số năm làm việc bằng năm hiện tại trừ năm vào làm)


12.	Viết chương trình thực hiện các yêu cầu sau:
- Khai báo lớp nhân sự với các thuộc tính: mã nhân viên, họ tên, năm sinh. Khai báo lớp công nhân kế thừa từ lớp nhân sự và thêm thuộc tính: ngày công.
- Xây dựng các phương thức: nhập, xuất cho các đối tượng nhân sự, công nhân.
- Nhập vào n công nhân. Thực hiện tính tiền thưởng cho công nhân 100.000 với mỗi ngày công làm thêm nếu ngày công lớn hơn 25. In ra màn hình danh sách các công nhân được thưởng và số tiền thưởng tương ứng.


13.	Viết chương trình thực hiện các yêu cầu sau:
- Khai báo lớp sinh viên với các thuộc tính: họ tên, năm sinh, điểm toán, điểm lý, điểm hóa.
- Xây dựng các phương thức: nhập, xuất, quy đổi điểm từ hệ 10 sang điểm chữ hệ 4
- Nhập vào n sinh viên. Thực hiện quy đổi điểm toán, điểm lý, điểm hóa sang điểm chữ hệ 4. In kết quả ra màn hình.


14.	Viết chương trình thực hiện các yêu cầu sau:
- Khai báo lớp người với các thuộc tính: họ tên, năm sinh. Khai báo lớp giảng viên kế thừa từ lớp người và thêm các thuộc tính: bộ môn công tác, môn đang giảng dạy.
- Xây dựng các phương thức: nhập, xuất cho các đối tượng người, giảng viên.
- Nhập vào n giảng viên. Sắp xếp lại và in ra màn hình danh sách giảng viên theo từng bộ môn công tác.


15.	Viết chương trình thực hiện các yêu cầu sau:
- Khai báo lớp hình vuông với thuộc tính: độ dài cạnh. Khai báo lớp hình chữ nhật kế thừa từ lớp hình vuông và thêm thuộc tính: độ dài cạnh thứ 2.
- Xây dựng các phương thức: nhập, xuất, tính chu vi, diện tích hình vuông, hình chữ nhật.
- Nhập vào n hình vuông. In ra màn hình hình vuông có diện tích lớn nhất và vị trí của hình vuông đó trong danh sách vừa nhập.
- Sắp xếp lại danh sách hình chữ nhật theo thứ tự giảm dần của chu vi. In kết quả ra màn hình.


16.	Viết chương trình thực hiện các yêu cầu sau:
- Khai báo lớp điểm với các thuộc tính: tọa độ x, tọa độ y. Khai báo lớp tam giác có thuộc tính 3 đỉnh là kiểu đối tượng điểm (lớp tam giác được gọi là lớp bao của lớp điểm) và thêm các thuộc tính: độ dài 3 cạnh.
- Xây dựng các phương thức: nhập, xuất, tính khoảng cách 2 điểm (dùng hàm bạn), áp dụng tính độ dài 3 cạnh của tam giác; nhập, xuất 3 đỉnh của tam giác.
- Nhập vào 1 tam giác. Kiểm tra tính chất của tam giác (thường, đều, vuông, cân, vuông cân). In kết quả ra màn hình.


17.	Viết chương trình thực hiện các yêu cầu sau:
- Khai báo lớp tam giác với các thuộc tính: độ dài 3 cạnh. Xây dựng hàm tạo không đối, hàm tạo có đối, hàm hủy một đối tượng tam giác.
- Xây dựng các phương thức: nhập, xuất, kiểm tra 3 cạnh có tạo thành tam giác hay không (nếu không tạo thành tam giác yêu cầu nhập lại).
- Nhập vào n tam giác. Thực hiện tính chu vi và diện tích các tam giác. Sắp xếp lại các tam giác theo thứ tự tăng dần của diện tích. In kết quả ra màn hình.


18.	Viết chương trình thực hiện các yêu cầu sau:
-Khai báo lớp SP1 mô tả các đối tượng là số phức với các thuộc tính: phần thực, phần ảo. 
- Xây dựng hàm tạo, hàm huỷ
- Xây dựng phương thức nhập số phức, in số phức, tính module số phức
- Xây dựng các toán từ >>, << để nhập, xuất số phức dưới dạng a+bi.
-Nạp chồng các toán tử: = (gán), < (nhỏ hơn), + (cộng), > (nhỏ hơn), == (so sánh bằng)
-Định nghĩa các toán tử +, -, *, / hai số phức. Thực hiện cộng, trừ, nhân, chia hai số phức và in kết quả ra màn hình.
-Viết chương trình chính ứng dụng lớp SP2 để nhập một danh sách các đối tượng là các số phức (tối đa 10 phần tử). 
+Tìm số phức có giá trị lớn nhất, nhỏ nhất. (theo module) 
+Tính tổng các số phức trong dãy số.
+Tìm số phức có giá trị lớn nhất và đếm xem có bao nhiêu số phức trong danh sách có giá trị bằng 3+4i.
+Sắp xếp lại danh sách đã nhập theo trật tự giảm dần của module.
